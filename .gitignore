# photo and video masters the site no longer serves — kept locally, never published
originals/

# local tooling and build output
node_modules/
package-lock.json
package.json
*.zip

.DS_Store
**/.DS_Store

# earlier versions of the site, kept on the Mac but never published
/index.html
/css/
/js/
/assets/
/v2/
*.xlsx
~$*

# local agent configuration
.claude/

# social media renders, kept on the Mac only
instagram-stories/
