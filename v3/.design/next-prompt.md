---
page: project content pass
layout: data
status: blocked-on-content
---
BLOCKED: project pages exist (`project.html?p=<slug>`) but need real content from Abdellah (SITE.md §5). Do not invent any fact.

**DESIGN SYSTEM:** copy DESIGN.md §7 here verbatim.

**Task:**
1. For each slug in `js/projects.js` (moonview, lost, hope, rayban-meta, aw-lab, busk-in-bozen, game-ground, lookbook), fill only the fields Abdellah supplies: `role`, `year`, `client`, `description`, `images` (paths under `assets/img/`), `video` (embed URL).
2. Export stills at 2400px long edge, JPEG quality 80, sRGB, like `assets/live/`.
3. Check each page at 1440 and 375 with `.design/harness.html`.
4. Bump `?v=` in all pages if CSS or JS changes.

**Also waiting on the client:** real KN logo file, Instagram post URLs, Billboard credit wording, professional email.
